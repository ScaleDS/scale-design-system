{
  "name": "@scale/design-system-mcp",
  "version": "1.0.0",
  "description": "MCP server for Scale Design System — exposes components, tokens, and patterns to AI agents",
  "type": "module",
  "bin": {
    "scale-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  }
}
